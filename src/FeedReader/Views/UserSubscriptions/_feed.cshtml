@model IEnumerable<FeedReader.Models.RSSFeed>

<h2>@ViewBag.Title</h2>
<div>
    <hr />
    <dl>
        @foreach (var item in Model)
        {
            <dt><a href=@item.link>@item.title</a></dt>
            <dd>@(new HtmlString(@item.description))</dd>
        }
    </dl>
</div>